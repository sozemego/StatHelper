const initialState = {
    scales: []
};

const scales = (state = initialState, action) => {
    switch (action.type) {

        default: return state;
    }
    return state;
};

export default scales;